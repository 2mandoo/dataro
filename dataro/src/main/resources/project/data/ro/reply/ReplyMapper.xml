<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="project.data.ro.reply.ReplyMapper">
	<insert id="insert" parameterType="project.data.ro.reply.ReplyVO">
		insert into preply(board_no,room_no,reply_no,member_id,member_no,content,reply_writedate)
		values (#{board_no},#{room_no}, #{reply_no}, #{member_id},#{member_no}, #{content}, NOW())
	</insert>
	
	<select id="list" parameterType="project.data.ro.reply.ReplyVO" resultType="project.data.ro.reply.ReplyVO">
		select * from preply where room_no = #{room_no}
	</select>
	<update id="update">
		update preply set content=#{content} where memer_no=#{member_no}
	</update>
	<delete id="delete">
		delete * from preply where reply_no=#{reply_no} and member_no = #{member_no};
	</delete>
</mapper>